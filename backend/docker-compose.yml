services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: importrad
      POSTGRES_PASSWORD: importrad
      POSTGRES_DB: importrad
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
