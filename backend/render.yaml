services:
  - type: web
    name: importrad-backend
    runtime: node
    buildCommand: cd backend && npm install && npx prisma migrate deploy && npm run build
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
